<div class="box17">
<p>
  <p>【口コミ】</p>
   <p><%= @post.content %></p>
  <div class="buy">
   <p>商品が気になる方は↓をクリック！</p>
    <a href="<%= @post.amazonurl %>" class="btn-stitch" target="_blank">購入ページへ</a>
  </div>
  <div class="post-user flex space">
    <p class="post-time"><%= @post.created_at.strftime("%Y-%m-%d %H:%M") %></p>
  </div>
 <p id="post-<%= @post.id %>">
  <% if @post.liked_by?(current_user) %>
    <%= render "like", post: @post %>
  <% else %>
    <%= render "dislike", post: @post %>
  <% end %>
</p>

<p><%= link_to "投稿一覧へ", posts_path %></p>
 </p>
</div>


