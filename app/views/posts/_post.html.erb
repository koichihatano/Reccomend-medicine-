<div class="container">
 <div class="row">

<div class="toplogo">
 <img src="toplogo.png" class="toplogo-image">
 </div>

<div class="ribbon10">
  <h3>オススメの薬をシェアしよう！</h3>
</div>

<div>
  <p>【内容】 <%= post.content %></p>
  <p id="post-<%= post.id %>">
    <% if @liked_post_ids.include?(post.id) %>
      <%= render "like", post: post %>
    <% else %>
      <%= render "dislike", post: post %>
    <% end %>
  </p>
 </div>
  <p>
    <%= link_to "詳細", post %>
    <% if current_user.id == post.user.id %>
      <%= link_to "編集", edit_post_path(post) %>
      <%= link_to "削除", post_path(post), method: :delete, data: { confirm: "削除しますか？" } %>
    <% end %>
  </p>
 
 </div>
</div>